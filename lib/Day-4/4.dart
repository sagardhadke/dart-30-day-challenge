//* 4. Create a bool isLoggedIn variable and toggle its value once, printing before and after.

void main(){

  bool isLoggedIn = false;
  print(isLoggedIn);
  isLoggedIn = !isLoggedIn;
  print(isLoggedIn);

}